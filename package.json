{
  "name": "nexus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm run install:all && npm run db:setup",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "db:setup": "cd server && npx prisma db push && node prisma/seed.js",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "render:build": "cd server && npm install && npx prisma db push && node prisma/seed.js && cd ../client && npm install && npm run build",
    "render:start": "cd server && NODE_ENV=production node src/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
